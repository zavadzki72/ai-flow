# Padrões Frontend

## Estrutura de Componentes

Descreva como os componentes são organizados:
- Estrutura de pastas (pages, components, hooks, etc.)
- Convenções de nomenclatura
- Quando criar componentes reutilizáveis

---

## Gerenciamento de Estado

Descreva a estratégia de estado:
- Ferramenta utilizada (Redux, Zustand, Context API, etc.)
- Quando usar estado global vs local
- Padrões de actions/reducers/stores

---

## Chamadas de API

Descreva como as chamadas de API são feitas:
- Camada de serviços/hooks
- Tratamento de erros
- Loading states

---

## Convenções de Nomenclatura

| Tipo | Padrão | Exemplo |
|------|--------|---------|
| Componente | `PascalCase` | `UserCard` |
| Hook | `use + PascalCase` | `useUserData` |
| Serviço | `camelCase + Service` | `userService` |
